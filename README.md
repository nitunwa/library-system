# library-system

Learning Spring MVC framework
##This a test project


#Git command:
git commit -a -m "jpa and nativ sql query"

git status
git add *

git commit -a -m "jpa and nativ sql query"

git push origin
 git remote -v